| 項目 | 値 |
|------|-----|
| フォント | Google Fonts「Source Sans Pro」 |
| コンテンツ幅 | 800px |　⇀★★ここは足されるの確認
(全幅以外の少し小さいところ) 

| サイドパディング | 30px |　⇀
| ブレイクポイント | 900px |　⇀全幅より少し小さいところ


/* コンテンツ幅を設定するための共通クラス */
.wrapper {
  max-width: 800px;  /* ← コンテンツ幅 */
  padding: 0 30px;   /* ← サイドパディング */
  margin: 0 auto;    /* ← 中央寄せ */
}

`★つまサイドパディングが横幅がつくので、800-60=740pxとなる。`
![](images/2026-01-20-12-47-47.png)

★　実表示幅	860px
```css
/* コンテンツ幅を設定するための共通クラス */
.wrapper {
  max-width: 800px;  /* ← コンテンツ幅 */
  padding: 0 30px;   /* ← サイドパディング */
  margin: 0 auto;    /* ← 中央寄せ */
}
```



以下のような画像があった場合
・コンテンツ幅は８６０PXである。
そのため、デザイン上は画像の配置が近いが、
８６０横幅一杯に広げる。中央に余白ができる

![](images/2026-01-20-11-13-08.png)


### Header

- 初期状態は `display: none` で非表示
- スクロール位置が**520px**に達した時に表示（jQuery fadeIn使用）
- スクロール位置は `$(window).scrollTop()` で取得

#### ハンバーガーメニュー

- クリック時に全画面メニューを上から下へスライド表示
- メニューは nav、ul、li タグで記述し、初期状態は非表示

### メインビジュアル（#mainvisual）

- 画像をFlexboxで横並び配置
- `overflow-x: hidden` で横スクロールを防止

-----------------------------------------------------------------

メインビジュアル、ヘッダー、INFORMATION、GALLERY、ACCESS、CONTACT、フッターなどのセクションがあります。

▢`ヘッダー`</span>

ヘッダーは、スクロール量に応じて出現する**追従型のナビゲーション**として実装します。

【構成と配置】
- `要素`: ロゴ、ハンバーガーメニュー
- `配置`: `position: fixed` を使用して画面の両端に固定。
- `重なり`: 他の要素（メインビジュアル等）より上に表示されるよう `z-index` の指定が必要。

【表示コントロール】
- `初期状態`: `display: none`（css）で非表示にしておく。
- `出現条件`: スクロール位置が **520px** に達した際に、jQueryの `fadeIn` を使ってふわっと表示させる。
- `判定`: `$(window).scrollTop()` を監視し、520以上なら表示、未満なら非表示にする制御が必要。

【ハンバーガーメニューの詳細】
- `見た目`: 3本線（spanタグ等で作成）のアイコン。
- `挙動`: クリックすると全画面メニューが**上から下へスライド**して表示される。
- `HTML構造`: セマンティックなHTMLにするため、メニュー部分は `nav` > `ul` > `li` > `a` の構造で作成する。
- `実装`: jQueryでクラスの付け外し（`toggleClass` 等）を行い、CSSまたはjQueryのアニメーションでスライドを表現する。

---

▢`メインビジュアル`</span>

スクロール量に合わせて画像が動的に変化する、視覚的効果の高いセクションです。

【レイアウト】
- `配置`: `Flexbox` を使用して画像を横に並べる。
- `はみ出し防止`: 画像拡大時に横スクロールバーが出ないよう、親要素に `overflow-x: hidden` を設定する。

【スクロールアニメーション】
- `演出`: スクロールに合わせて画像サイズを変更する。PCとSPで挙動が逆になる点に注意。
- `PC表示`: スクロールするほど画像が**拡大**する（widthを増加させる）。
- `SP表示`: スクロールするほど画像が**縮小**する（widthを減少させる）。
- `計算`: `$(window).scrollTop()` の値を利用して、リアルタイムにCSSの `width` を計算・更新する。

---

`INFORMATION`</span>

【構成】
- `タグ`: `section` で囲み、見出しは `h2` を使用。
- `テキスト`: フォントサイズや色が異なる箇所は `span` タグで個別にスタイルを当てる。

【出現アニメーション】
- `挙動`: 画面外（下）から要素が入ってきたタイミングで、ふわっと表示させる。
- `実装`: jQueryプラグインの **inview** を使用して、要素が可視領域に入ったことを検知する。

---

▢`GALLERY`</span>

【レイアウト】
- `配置`: 画像を交互（左・右・左...）に配置する。
- `手法`: CSSの `nth-child(odd)`（奇数・左寄せ）と `nth-child(even)`（偶数・右寄せ）を活用して、効率的にスタイリングする。

【アニメーション】
- `挙動`: INFORMATIONと同様に、画面内に入ったタイミングで下からふわっと表示（inviewプラグイン使用）。

---

▢`サイドボタン`</span>

PC表示時のみ、特定のセクション間でのみ出現する浮遊ボタンです。

【表示条件】
- `出現`: **GALLERYタイトル**が画面に入ったタイミングで、右からスライドして表示。
- `消失`: **ACCESSタイトル**が画面に入ったタイミングで、スライドして消える。
- `レスポンシブ`: スマホ（SP）では `display: none` で完全に非表示。

---

▢`ACCESS`</span>

セクションの背景が、スクロール位置によって全画面画像へ切り替わる特殊な演出があります。

【背景切り替え】
- `演出`: **ACCESSタイトル**が来たら背景をフェードイン表示、**CONTACTタイトル**が来たらフェードアウト。
- `背景の固定`: `position: fixed`、`width: 100vw`、`height: 100vh` を使い、コンテンツの背後で常に全画面表示されるようにする。

---

▢`CONTACT`</span>

【ボタンの装飾】
- `矢印`: CSSの疑似要素（`::before` で直線、`::after` で先端）を組み合わせて作成する。
- `ホバー効果（PC）`: 透過度を変更し、矢印を少し右に移動（`right` 値の変更）させるアニメーションを加える。

---

▢`フッター`</span>

【構成とレイアウト】
- `要素`: 左側にコピーライト等のテキスト、右側にナビゲーションリストを配置。
- `配置`: `Flexbox` を使用して両端に振り分ける。



ーーーーーーーーーーーーーーーーーーーーーーーーーーーー
【設計思想・実務】

'【方針】'
お深いところはクロードコード・浅いところは、コパイロット
設計はしっかりする。コードはコメントをかいてどんどん
コパイロットで修正していく。


🎯 本当に最初にやるべきこと（普遍版）

🎯  カンプ全体を見て共通パーツを洗い出す
・コンテンツ幅とか。
・サブタイトルとか。両脇に線があるタイトルなど

◎
【INFORMATION、GALLERY、ACCESS、CONTACTなどのメッセージ　】


🎯  セクション構造を決める
<header>      ← ロゴ・ナビ
<main>
  <section id="hero"> ← メインビジュアル
  <section id="concept"> ← コンセプト
</main>
<footer>

◎１１セクション
✅ 定数定義・共通設定
✅ header　　　(ハンバーガー・左上のロゴ)
      └ ✅ navi (ハンバーガーの中身)
✅ mainvisual　（画像３枚）
✅ gallery
✅ site-title
✅ information
✅ side-btn
✅ access
✅ bg
✅ footer


🎯 fixed要素があるか確認
・. 迷いがなくなる
main {
  padding-top: 80px;  /* ヘッダー分だけ下げる */
}
・ **z-index地獄を防ぐ
ヘッダー（fixed） z-index: 100
モーダル（fixed） z-index: 200
サイドボタン（fixed） z-index: 50 
![](images/2026-01-21-07-27-16.png)
![](images/2026-01-21-07-29-24.png)

```css
/* z-index階層の全体像 */
.bg                      z-index: -1   /* 背景（最背面） */
(通常コンテンツ)           z-index: 0    /* デフォルト */
.side-btn                z-index: 50   /* サイドボタン */
header                   z-index: 100  /* 固定ヘッダー */
.hamburger-menu          z-index: 150  /* 全画面メニュー */
 nav                      z-index: 500 /* /ハンバーガー押下時の詳細 */

★★★ AIで各セクション・固定メニューを洗い出して
インデックスの順位をつける。`ここは全体を読めるクロードコードでだす。
・メリット
・PC,SPの仕様までかいてくれる。




🎯  （アニメーションがあれば）トリガー整理
・ HTMLの構造が変わるから
（「アニメーションつけて」と言われると、全部のセクションにクラスを追加する羽目になる。）
・CSSの初期状態を決める必要があるから
.fade-up {
  opacity: 0;   /* 最初は透明 */
  transform: translateY(30px);  /* 最初は下にズレてる */
}

`【コメント】`
`【セマンティック】`



★大事・実務テンプレ-----------------------------------
【設計思想・実務】
以下を決める。
★sankou フォルダに以下の資料をおく
・デザインカンプの画像
・仕様（以下にかかれた内容）
・セクションをAIにかかせる。



🎯 本当に最初にやるべきこと（普遍版）
🎯 仕様書の命名規則・ファイル構成を確認
🎯  カンプ全体を見て共通パーツを洗い出す
・コンテンツ幅とか。
・サブタイトルとか。両脇に線があるタイトルなど 

🎯  セクション構造を決める
<header>      ← ロゴ・ナビ
<main>
  <section id="hero"> ← メインビジュアル
  <section id="concept"> ← コンセプト
</main>
<footer>

🎯  fixed要素があるか確認
・. 迷いがなくなる
main {
  padding-top: 80px;  /* ヘッダー分だけ下げる */
}
・ **z-index地獄を防ぐ
ヘッダー（fixed） z-index: 100
モーダル（fixed） z-index: 200
サイドボタン（fixed） z-index: 50 
![](images/2026-01-21-07-27-16.png)
![](images/2026-01-21-07-29-24.png)


🎯 （アニメーションがあれば）トリガー整理
・ HTMLの構造が変わるから
（「アニメーションつけて」と言われると、全部のセクションにクラスを追加する羽目になる。）
・CSSの初期状態を決める必要があるから
.fade-up {
  opacity: 0;   /* 最初は透明 */
  transform: translateY(30px);  /* 最初は下にズレてる */
}

`【コメント】`
`【セマンティック】`
-----------------------------------------

---

## 📋 セクション別処理・アニメーション・レイアウト一覧

### 1️⃣ header
- **レイアウト**: position: fixed（ロゴ左上・ハンバーガー右上）
- **初期状態**: display: none
- **アニメーション**: scrollTop >= 520px で fadeIn表示
- **JS処理**: $(window).scrollTop()で監視
- **flex**: なし（absolute配置）

### 2️⃣ navi（ハンバーガーメニュー）
- **レイアウト**: position: fixed（全画面）
- **初期状態**: opacity: 0 + pointer-events: none
- **アニメーション**: ハンバーガークリックで上→下スライド
- **JS処理**: toggleClass('open')
- **flex**: あり（縦並び・中央揃え）
  - `flex-direction: column`
  - `justify-content: center`

### 3️⃣ mainvisual
- **レイアウト**: width: 100%, height: 100vh
- **親要素**: overflow-x: hidden（横スクロール防止）
- **アニメーション**:
  - PC: スクロール↓で画像拡大
  - SP: スクロール↓で画像縮小
- **JS処理**: scrollTop値で width動的変更
- **flex**: あり（画像3枚横並び）
  - `.mainvisual_imgwrapper { display: flex; justify-content: center; }`
  - `.mainvisual_img { width: 33%; }`

### 4️⃣ gallery
- **レイアウト**: コンテンツ幅800px + padding 30px
- **アニメーション**: 画面内入ったら下→上 fadeIn（inviewプラグイン）
- **JS処理**: inview.js
- **flex**: あり（画像交互配置）
  - `.gallery_flex { display: flex; flex-wrap: wrap; justify-content: space-between; }`
  - `.gallery_item { width: 48%; }`
  - `nth-child(odd)`: text-align: left
  - `nth-child(even)`: text-align: right

### 5️⃣ site-title
- **レイアウト**: 中央配置
- **アニメーション**: 下→上 fadeIn（inview）
- **flex**: なし

### 6️⃣ information
- **レイアウト**: コンテンツ幅内
- **アニメーション**: 下→上 fadeIn（inview）
- **JS処理**: inview.js
- **flex**: なし
- **備考**: span でフォントサイズ・色調整

### 7️⃣ side-btn
- **レイアウト**: position: fixed（PC右下）
- **表示条件**:
  - GALLERYタイトル表示時: 右→左スライドイン
  - ACCESSタイトル表示時: 左→右スライドアウト
- **JS処理**: inview.js でタイトル監視
- **flex**: なし
- **SP対応**: display: none

### 8️⃣ access
- **レイアウト**: コンテンツ幅内
- **アニメーション**: 下→上 fadeIn（inview）
- **背景切替**:
  - ACCESSタイトル表示時: .bg fadeIn
  - CONTACTタイトル表示時: .bg fadeOut
- **flex**: なし

### 9️⃣ bg（ACCESS背景画像）
- **レイアウト**: position: fixed, width: 100vw, height: 100vh
- **初期状態**: opacity: 0
- **アニメーション**: ACCESS/CONTACTタイトルで fadeIn/Out
- **JS処理**: inview.js
- **z-index**: -1（最背面）
- **flex**: なし

### 🔟 contact
- **レイアウト**: コンテンツ幅内
- **アニメーション**: 下→上 fadeIn（inview）
- **疑似要素**: 矢印（::before直線 + ::after先端）
- **hover効果**: opacity変更 + 矢印右移動
- **flex**: なし

### 1️⃣1️⃣ footer
- **レイアウト**: 画面全幅
- **flex**: あり（左右両端配置）
  - `display: flex`
  - `justify-content: space-between`
- **構成**: 左=コピーライト / 右=ナビリスト

---

## 🎯 flex使用箇所まとめ
| セクション | flex目的 | 主要プロパティ |
|-----------|---------|--------------|
| navi | 縦並び中央 | flex-direction: column |
| mainvisual | 画像3枚横並び | justify-content: center |
| gallery | 画像交互配置 | flex-wrap: wrap, space-between |
| footer | 左右両端配置 | space-between |

---

## 🎬 アニメーショントリガーまとめ
| 対象 | トリガー | 手法 |
|-----|---------|------|
| header | scrollTop >= 520px | jQuery fadeIn |
| navi | ハンバーガークリック | toggleClass |
| mainvisual | scroll中 | width動的計算 |
| gallery/info/access/contact | 画面内表示 | inview.js |
| side-btn | GALLERY/ACCESSタイトル | inview.js |
| bg | ACCESS/CONTACTタイトル | inview.js |

